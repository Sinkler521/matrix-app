First of all, you have to install dependencies. Use
``` 
npm install
```

To run in dev mode, use
```
npm run dev
```

to make a build for production, and then run it, use
```
npm run build
npm run start
```